export module physics;

import app;

export class PhysicsPlugin : public Plugin {
public:
    void build(App& app) override {
    }

    void execute(App& app) override {
    }

    void cleanup(App& app) override {
    }
};
